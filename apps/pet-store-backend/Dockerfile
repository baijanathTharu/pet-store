# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build pet-store-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t pet-store-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system pet-store-backend && \
          adduser --system -G pet-store-backend pet-store-backend

COPY dist/apps/pet-store-backend pet-store-backend
RUN chown -R pet-store-backend:pet-store-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix pet-store-backend --omit=dev -f install

CMD [ "node", "pet-store-backend" ]
