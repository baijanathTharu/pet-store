# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build pet-store-fastify`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t pet-store-fastify`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system pet-store-fastify && \
          adduser --system -G pet-store-fastify pet-store-fastify

COPY dist/apps/pet-store-fastify pet-store-fastify
RUN chown -R pet-store-fastify:pet-store-fastify .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix pet-store-fastify --omit=dev -f install

CMD [ "node", "pet-store-fastify" ]
